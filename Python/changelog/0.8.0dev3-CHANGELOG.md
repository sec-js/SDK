# Changelog

All notable changes to this project will be documented in this file.

## [0.8.0dev3]

### 🚀 Features

- Client initialization now supports:
    - the **base_url** parameter, e.g. `https://public.intelx.io`, `https://free.intelx.io`
    - proxy support via the **proxies** parameter, see https://requests.readthedocs.io/en/latest/user/advanced/#proxies
    - the **verify** parameter, see https://requests.readthedocs.io/en/latest/user/advanced/#ssl-cert-verification
- Added method **INTEL_EXPORT**, **exportfromsearch** to the `intelxapi` client and *--exportfromsearch* parameter to the command line client
- Added a `.devcontainer` with Dockerfile for easily running a Python container in VS Code or Docker
- Command-line client now supports the **--proxy-insecure** switch to disable TLS verification
- Added **reverse_domain** method for reverse domain searches
- Added support for a `.env` file

### 🚜 Refactor

- `IdentityService` class now properly inherits from the `intelx` class
- Previously dismissed records are now captured when a `status: 2` response is returned in `export_accounts`

### 📚 Documentation

- OpenAPI specification in [openapi.yaml](https://github.com/IntelligenceX/SDK/blob/master/openapi.yaml) and [openapi.md](https://github.com/IntelligenceX/SDK/blob/master/openapi.md)
